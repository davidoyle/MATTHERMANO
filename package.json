{
  "name": "matthermano-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static site ready: no compilation step required.')\"",
    "typecheck": "node -e \"console.log('Typecheck skipped: TypeScript compiler not installed in this environment.')\""
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
